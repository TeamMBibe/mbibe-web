import React, { Component } from 'react';
import {observer} from "mobx-react";

const MemberProfileComponent = observer(class MemberProfileComponent extends Component {

  constructor(props) {
      super(props);
  }

  render() {

      return (
          <div>
              
          </div>
      );
    }
  });
};



export default MemberProfileComponent;
